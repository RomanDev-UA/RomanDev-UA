🛠️ ШПАРГАЛКА RomanDev AI
💻 КОМАНДЫ ТЕРМИНАЛА
python agent.py — Запустить программу.

pip install pandas openpyxl — Установить модули для Excel.

clear — Очистить экран консоли.

Ctrl + C — Принудительно остановить зависший код.

📦 РАБОТА С GITHUB (Сохранение)
git add . — Подготовить все изменения.

git commit -m "описание" — Запечатать сохранение.

git push origin main — Отправить в облако на GitHub.

🐍 ОСНОВЫ PYTHON
name = "Труба" — Текст (String).

price = 150.5 — Число с точкой (Float).

qty = 10 — Целое число (Integer).

print(f"Итого: {x}") — Вывод текста с переменной.

✂️ ОБРАБОТКА ТЕКСТА
.split(',') — Разрезать строку по запятой.

.strip() — Убрать пробелы по краям.

.lower() — Все буквы маленькие.

📁 ФАЙЛЫ И ТАБЛИЦЫ
import pandas as pd — Подключить таблицы.

df.to_excel("file.xlsx") — Сохранить в Excel.

open("log.txt", "a") — Дописать строку в журнал.
